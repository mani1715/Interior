'use client';
import {useId,useRef,useState} from 'react';import type {NavItem} from '@/lib/portfolio/template-contract';import styles from './LuxuryTemplate.module.css';
export function LuxuryHeader({mainId,name,links,cta}:{mainId:string;name:string;links:NavItem[];cta?:{href:string;label:string}}){const[open,setOpen]=useState(false);const b=useRef<HTMLButtonElement>(null);const id=useId();return <header className={styles.header} onKeyDown={e=>{if(e.key==='Escape'&&open){setOpen(false);b.current?.focus();}}}><a className={styles.wordmark} href={`#${mainId}`}><span aria-hidden="true">✦</span>{name}</a>{(links.length>0||cta)&&<button className={styles.menu} ref={b} type="button" aria-expanded={open} aria-controls={id} onClick={()=>setOpen(!open)}>{open?'Close':'Menu'}</button>}{(links.length>0||cta)&&<nav id={id} className={styles.nav} data-open={open} aria-label="Portfolio"><span>Portfolio index</span>{links.map(x=><a key={x.sectionId} href={x.anchor} onClick={()=>setOpen(false)}>{x.label}</a>)}{cta&&<a className={styles.navCta} href={cta.href} onClick={()=>setOpen(false)}>{cta.label} <span aria-hidden="true">↗</span></a>}</nav>}</header>}
