import { PortfolioMotion } from '../PortfolioMotion';
import { visualPalette } from '../visual-palette';
import{useId}from'react';import type{PortfolioTemplateProps}from'@/lib/portfolio/template-contract';import{LuxuryHeader}from'./LuxuryHeader';import{LuxurySection}from'./LuxurySections';import{safeUrl,text,usable}from'./luxury';import styles from'./LuxuryTemplate.module.css';
export function LuxuryTemplate(p:PortfolioTemplateProps){const mainId=`luxury-main-${useId()}`;const list=[...p.visibleSections].sort((a,b)=>a.displayOrder-b.displayOrder).filter(s=>usable(s,p));const used=new Set<string>();const planned=list.map((s,i)=>{const n=p.navigationSettings.navItems.find(x=>x.sectionId===s.sectionId);const raw=n?.anchor.replace(/^#/,'')||s.sectionType.toLowerCase().replace(/_/g,'-');const base=/^[a-z][a-z0-9-]*$/i.test(raw)?raw:`section-${i}`;const id=used.has(base)?`${base}-${i}`:base;used.add(id);return{section:s,id};});const anchors=new Set(planned.map(x=>x.id));const rendered=planned.filter(x=>x.section.sectionType!=='CTA'||safeUrl(x.section.content.buttonLink,anchors));const links=p.navigationSettings.navItems.flatMap(n=>{const t=rendered.find(x=>x.section.sectionId===n.sectionId);return t?[{...n,anchor:`#${t.id}`}]:[]});const cta=p.navigationSettings.showPrimaryCta?safeUrl(p.navigationSettings.primaryCtaAnchor,anchors):undefined;const hero=rendered.find(x=>x.section.sectionType==='HERO');const name=text(p.studioName)||'Portfolio';return<div className={styles.root} style={{...visualPalette(p, ['#F8F5EF', '#292622', '#9A7652'])}} data-font={p.fontPairing||'SYSTEM_SANS'} data-mobile={p.isMobilePreview||undefined}><a className={styles.skip} href={`#${mainId}`}>Skip to portfolio content</a><LuxuryHeader mainId={mainId} name={name} links={links} cta={cta?{href:cta,label:p.navigationSettings.primaryCtaLabel}:undefined}/><main id={mainId} tabIndex={-1}>{!hero&&<section className={styles.section}><h1>{name}</h1></section>}{rendered.map(x=><LuxurySection key={x.section.sectionId} section={x.section} portfolio={p} id={x.id} anchors={anchors} isHero={x===hero}/>)}</main><PortfolioMotion mainId={mainId}/><footer className={styles.footer}><div><strong><span aria-hidden="true">✦</span>{name}</strong><p>{[p.studioCity,p.studioState].filter(Boolean).join(', ')}</p></div></footer></div>}
