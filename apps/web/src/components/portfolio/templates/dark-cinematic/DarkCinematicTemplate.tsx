import { PortfolioMotion } from '../PortfolioMotion';
import { visualPalette } from '../visual-palette';
import{useId}from'react';import type{PortfolioTemplateProps}from'@/lib/portfolio/template-contract';import{DarkCinematicHeader}from'./DarkCinematicHeader';import{DarkCinematicSection}from'./DarkCinematicSections';import{safeUrl,text,usable}from'./dark-cinematic';import styles from'./DarkCinematicTemplate.module.css';export function DarkCinematicTemplate(p:PortfolioTemplateProps){const mainId=`cinematic-main-${useId()}`;const list=[...p.visibleSections].sort((a,b)=>a.displayOrder-b.displayOrder).filter(s=>usable(s,p));const used=new Set<string>();const planned=list.map((s,i)=>{const n=p.navigationSettings.navItems.find(x=>x.sectionId===s.sectionId);const raw=n?.anchor.replace(/^#/,'')||s.sectionType.toLowerCase().replace(/_/g,'-');const id=used.has(raw)?`${raw}-${i}`:raw;used.add(id);return{section:s,id}});const anchors=new Set(planned.map(x=>x.id));const rendered=planned.filter(x=>x.section.sectionType!=='CTA'||safeUrl(x.section.content.buttonLink,anchors));const links=p.navigationSettings.navItems.flatMap(n=>{const x=rendered.find(y=>y.section.sectionId===n.sectionId);return x?[{label:n.label,anchor:`#${x.id}`}]:[]});const cta=p.navigationSettings.showPrimaryCta?safeUrl(p.navigationSettings.primaryCtaAnchor,anchors):undefined;const hero=rendered.find(x=>x.section.sectionType==='HERO');const name=text(p.studioName)||'Studio';return <div className={styles.root} style={{...visualPalette(p, ['#141414', '#FAF8F5', '#B88A5A'])}} data-font={p.fontPairing || 'SYSTEM_SANS'}><a className={styles.skip} href={`#${mainId}`}>Skip to content</a><DarkCinematicHeader mainId={mainId} name={name} links={links} cta={cta?{href:cta,label:p.navigationSettings.primaryCtaLabel}:undefined}/><main id={mainId} tabIndex={-1}>{!hero&&<section className={styles.section}><div className={styles.content}><h1>{name}</h1></div></section>}{rendered.map(x=><DarkCinematicSection key={x.section.sectionId} {...x} portfolio={p} anchors={anchors} isHero={x===hero}/>)}</main><PortfolioMotion mainId={mainId}/><footer className={styles.footer}><strong>◆ {name}</strong><span>{[p.studioCity,p.studioState].filter(Boolean).join(' / ')}</span></footer></div>}
