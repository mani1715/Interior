'use client';
import {useEffect,useRef,useState} from 'react';
import styles from './ArchitecturalTemplate.module.css';
export function ArchitecturalHeader({mainId,name,links,cta}:{mainId:string;name:string;links:{label:string;anchor:string}[];cta?:{href:string;label:string}}){const [open,setOpen]=useState(false);const ref=useRef<HTMLButtonElement>(null);useEffect(()=>{if(!open)return;const f=(e:KeyboardEvent)=>{if(e.key==='Escape'){setOpen(false);ref.current?.focus();}};document.addEventListener('keydown',f);return()=>document.removeEventListener('keydown',f)},[open]);return <header className={styles.header}><a className={styles.brand} href={`#${mainId}`}><span aria-hidden="true">A/</span><span>{name}</span></a><button ref={ref} className={styles.menu} aria-expanded={open} aria-controls={`${mainId}-nav`} onClick={()=>setOpen(x=>!x)}>{open?'Close':'Index'}</button><nav id={`${mainId}-nav`} className={`${styles.nav} ${open?styles.navOpen:''}`} aria-label="Portfolio sections">{links.map((l,i)=><a key={l.anchor} href={l.anchor} onClick={()=>setOpen(false)}><b>{String(i+1).padStart(2,'0')}</b>{l.label}</a>)}{cta&&<a className={styles.navCta} href={cta.href} onClick={()=>setOpen(false)}>{cta.label}</a>}</nav></header>}
