'use client';import{useEffect,useRef,useState}from'react';import styles from './WarmNaturalTemplate.module.css';export function WarmNaturalHeader({mainId,name,links,cta}:{mainId:string;name:string;links:{label:string;anchor:string}[];cta?:{href:string;label:string}}){const[open,setOpen]=useState(false);const ref=useRef<HTMLButtonElement>(null);useEffect(()=>{if(!open)return;const f=(e:KeyboardEvent)=>{if(e.key==='Escape'){setOpen(false);ref.current?.focus()}};document.addEventListener('keydown',f);return()=>document.removeEventListener('keydown',f)},[open]);return <header className={styles.header}><a className={styles.brand} href={`#${mainId}`}><span aria-hidden="true">◦</span>{name}</a><button ref={ref} className={styles.menu} aria-expanded={open} aria-controls={`${mainId}-nav`} onClick={()=>setOpen(x=>!x)}>{open?'Close':'Menu'}</button><nav id={`${mainId}-nav`} className={`${styles.nav} ${open?styles.open:''}`} aria-label="Portfolio sections">{links.map(l=><a key={l.anchor} href={l.anchor} onClick={()=>setOpen(false)}>{l.label}</a>)}{cta&&<a className={styles.navCta} href={cta.href} onClick={()=>setOpen(false)}>{cta.label}</a>}</nav></header>}
